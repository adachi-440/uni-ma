require 'rails_helper'

RSpec.describe Item, type: :model do
  describe 'バリデーションのチェック' do
    let(:item) { create(:item) }
    context '正しい値が入力がされている場合' do
      it '出品が完了' do

      end
    end
  end
end
